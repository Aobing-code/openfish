version: "3.8"

services:
  fishrouter:
    build: .
    image: fishrouter:latest
    container_name: fishrouter
    ports:
      - "8080:8080"
    volumes:
      - ./config.json:/app/config.json
    restart: unless-stopped
